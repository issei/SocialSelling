# Copie para .env e preencha. O .env NUNCA e versionado (ver .gitignore).
# Unicos custos variaveis do PoC: tokens destas duas APIs.

# Tavily — sensor exclusivo de busca (Modulo M1)
TAVILY_API_KEY=

# Gemini — motor cognitivo (Modulos M2 / M3 / M5)
GEMINI_API_KEY=

APOLLO_API_KEY=